import React from 'react';
import styles from './page.module.css';

function Page(props) {
    return (
        <div className={styles.wrapper}>
            Logo Animation
        </div>
    );
}

export default Page;